# shared-types

Ainda não implementado. Tipos de domínio (ex.: `ChordEvent`, `KeySegment`)
compartilhados entre apps/web e services/* em TypeScript, para não duplicar
essas definições em lugares diferentes. Ver `docs/STRUCTURE.md` §1.
